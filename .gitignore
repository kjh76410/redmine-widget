# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# 런타임 상태 파일 (사용자별로 값이 달라지므로 추적 제외)
widget_position.json
redmine_seen_issues.json
redmine_resolved_by_version_cache.json
redmine_team_progress_cache.json

# 레드마인 API 키 (비밀번호와 같은 급 - 절대 커밋하지 않는다)
# 파일은 각자 로컬에 두고, 위젯이 실행 중에 읽어 쓴다(redmine_api.py 참고).
redmine_api_key.txt
team_redmine_api_key.txt
